export interface User {
    id: string;
    username: string;
    email: string;
   
}

export interface Account {
    id: string;
    username: string;
    email: string;
    password: string;
    // createdAt?: Date;
    // updatedAt?: Date;
}

